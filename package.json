{
  "name": "deta-random-cron",
  "version": "1.0.0",
  "description": "Set random cron to deta micro",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "vitest run --globals --dir=test --isolate=true",
    "jest": "jest",
    "btt": "node baseTimeTest.mjs",
    "btti": "node baseTimeTestIt.mjs",
    "esb": "esbuild ./lib/ --bundle --platform=node --external:./node_modules/* --outfile=lib.js --target=node14"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "deta": "^1.1.0",
    "node-fetch": "^3.2.3"
  },
  "devDependencies": {
    "better-typescript-lib": "^2.1.0",
    "esbuild": "^0.14.42",
    "jest": "^28.1.0",
    "typescript": "^4.7.2",
    "vitest": "^0.12.6"
  }
}
