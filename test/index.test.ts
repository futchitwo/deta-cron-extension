import { cron } from '../lib/index.js';

describe('Cron', () => {
  it('Initialization', () => {
    
    expect(0).toEqual(0);
  });

  it('Normal event', () => {
    
    expect(0).toEqual(0);
  });

  it('Past event', () => {
    
    expect(0).toEqual(0);
  });
});